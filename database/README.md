# database/

Esquemas, migraciones y políticas RLS de Supabase — fuente de verdad del modelo de datos.

**Stack:** Supabase (PostgreSQL), Row Level Security (RLS).

**Contenido futuro:**
- `migrations/` — archivos SQL ordenados por fecha
- `schema.sql` — esquema completo de tablas y relaciones
- `rls.sql` — políticas de seguridad por rol y tenant
- `seed.sql` — datos iniciales para desarrollo/testing
